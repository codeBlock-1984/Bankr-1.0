import React from 'react';

const TopPlaceholder = ({ body }) => (
  <div className="box trans-box l-flex l-flex-row placeholder-top">
    <h2 className="placeholder-text">{body}</h2>
  </div>
);

export default TopPlaceholder;
