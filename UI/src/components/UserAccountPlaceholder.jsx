import React from 'react';

const UserAccountPlaceholder = ({ body }) => (
  <div className="placeholder-user-account l-flex l-flex-col">
    <h2 className="placeholder-text">{body}</h2>
  </div>
);

export default UserAccountPlaceholder;
