import React from 'react';

const BottomPlaceholder = ({ body }) => (
  <div className="account-wrapper l-flex l-flex-row placeholder-bottom">
    <h2 className="placeholder-text">{body}</h2>
  </div>
);

export default BottomPlaceholder;
